name: Build
on:
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "dev" ]
  workflow_dispatch:
jobs:
  linux:
    uses: externpro/externpro/actions/build-linux.yml@main
    with:
      cmake-workflow-preset: LinuxRelease
      runon: ubuntu-latest
    secrets: inherit
  linux-arm64:
    uses: externpro/externpro/actions/build-linux.yml@main
    with:
      cmake-workflow-preset: LinuxRelease
      runon: ubuntu-24.04-arm
    secrets: inherit
  windows:
    uses: externpro/externpro/actions/build-windows.yml@main
    with:
      cmake-workflow-preset: WindowsRelease
    secrets: inherit
